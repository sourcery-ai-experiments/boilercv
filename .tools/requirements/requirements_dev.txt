# Requirements for the dev environment
# * ---------------------------------------------------------------------------------- *
# ? For data/scripts/filt.py
pandocfilters==1.5.0
# ? For using the latest version of this core package during development
-e ../boilercore
# ? Used to execute Jupyter notebooks in documentation
colorcet==3.0.1
geopandas==0.14.2
pyjanitor==0.26.0
shapely==2.0.2
trackpy==0.6.1
# * ---------------------------------------------------------------------------------- *
# Install tests package so modules can be run in debugging
-e tests/.
# * ---------------------------------------------------------------------------------- *
# * # Experiment
# * # ? Tracked in `requirements.txt` since it's part of package deps
# * ipykernel
# * ---------------------------------------------------------------------------------- *
# Debug
debugpy==1.8.0
# For `pythonstartup.py`
rich==13.7.0
# Format
sourcery-cli==1.15.0
# Profiling
snakeviz==2.2.0
# Pre-commit
pre-commit==3.6.0
# For local publishing to PyPi with flit
keyring==24.3.0

-r requirements_both.txt
