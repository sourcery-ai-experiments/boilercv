# List project dependencies here. A workflow ports them to `pyproject.toml`. This is needed because Dependabot does not currently support `flit`-flavored `pyproject.toml`.
dvc[gs]==2.52.0
loguru==0.6.0
matplotlib==3.7.1
opencv-contrib-python==4.7.0.72
pandas==2.0.0
pandera==0.14.5
pillow==9.5.0
pyarrow==11.0.0
pydantic==1.10.7
pyjanitor==0.24.0
pytz==2023.3
pyyaml==6.0
ruamel.yaml==0.17.21
scikit-image==0.20.0
scipy==1.10.1
seaborn==0.12.2
# * pyqtgraph and its extra dependencies
pyqtgraph==0.13.2
# ? https://github.com/pyqtgraph/pyqtgraph/issues/2672
pyside6==6.4.3,<6.5
# * xarray and its extra dependencies
# ? See: https://docs.xarray.dev/en/stable/getting-started-guide/installing.html
# ? Can't specify `accel` extras yet, see: https://github.com/numba/numba/issues/8304
# TODO: Revisit `accel` extras now that numba/numba#8304 is closed
# ! Install xarray with nodeps until Pandas 2 is allowed
# TODO: Take xarray out of nodeps when reverting
# xarray==2023.3.0
bottleneck==1.3.7
netcdf4==1.6.3
# ! Once Pandas 2 is allowed, remove these manually-specified deps
python-dateutil==2.8.2
